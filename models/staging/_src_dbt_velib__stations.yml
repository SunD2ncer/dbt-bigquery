version: 2

sources:
  - name: raw
    database: dbt-gcp-8abd
    schema: dbt_velib
    config:
      freshness:
        warn_after:
          count: 7
          period: day
        error_after:
          count: 14
          period: day
      loaded_at_field: extracted_at
    
    tables:
      - name: raw_velib_stations